{#
/**
 * @File
 * Default template for the rendered quicklink sets.
 *
 * Available variables:
 * - quicklins: The rendered quicklinks based on options provided to the theme
 *          function
 * - is_inline: TRUE if selected to be an inline-list. Defaults to FALSE
 * - use_link_text: TRUE if selected display the captured link text. Defaults to
 *                  TRUE.
 * - use_link_url: TRUE if selected to display the URL in the list. Defaults to
 *                 FALSE.
 *
 * @see rw_quicklinks_preprocess_quicklinks__list()
 *
 * @ingroup themeable
 */
#}
{{ quicklinks }}

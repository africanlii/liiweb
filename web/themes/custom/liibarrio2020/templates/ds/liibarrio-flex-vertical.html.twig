<{{ outer_wrapper }}{{ attributes }}>
  <{{ content_wrapper }}{{ content_attributes.addClass('d-flex flex-column justify-content-between h-100') }}>
    {{ content }}
  </{{ content_wrapper }}>
</{{ outer_wrapper }}>
<{{ outer_wrapper }}{{ attributes.addClass('row') }}>

  {{ title_suffix.contextual_links }}

  <div class="mobile-only open-filter">
    <a href="#" role="button" class="btn btn-green" id="toggle_filters" aria-controls="sidebar_first" aria-expanded="false">Open Details</a>
  </div>

  {# Main #}

  <{{ left_wrapper }}{{ left_attributes.addClass('layout-sidebar layout-sidebar--first col') }} id="sidebar_first">
    <aside class="content-aside--inner-wrapper section">
      <div class="sidebar-block drop-shadow top-right-radius">
        <div class="beige top-right-radius">
          <h3>Details</h3>
        </div>
        <div class="white thin-border">
          {{ left.field_case_number }}
          {{ left.field_court_name }}
          {{ left.field_judge }}
          {{ left.field_law_report_citations }}
          {{ left.field_media_neutral_citation }}
        </div>
      </div>
      <div class="sidebar-block drop-shadow top-right-radius">
        <div class="beige top-right-radius">
          <h3>Related</h3>
        </div>
        <div class="white thin-border">
          {{ left['dynamic_block_field:node-similar_judgments'] }}
        </div>
      </div>
      <div class="sidebar-block drop-shadow top-right-radius">
        <div class="beige top-right-radius">
          <h3>Flynote</h3>
        </div>
        <div class="white thin-border">
          {{ left.field_flynote }}
          {{ left.field_flynote_local }}
        </div>
      </div>
      <div class="sidebar-block drop-shadow top-right-radius">
        <div class="beige top-right-radius">
          <h3>PDF</h3>
        </div>
        <div class="white thin-border">
          {{ left.field_files }}
        </div>
      </div>
      <div class="sidebar-block drop-shadow top-right-radius">
        <div class="beige top-right-radius">
          <h3>Sharing</h3>
        </div>
        <div class="thin-border white">
          {{ left['dynamic_token_field:node-share_links'] }}
        </div>
      </div>
      
    </aside>
  </{{ left_wrapper }}>

  <{{ right_wrapper }}{{ right_attributes.addClass('layout-main col') }} id="content">
    {# Header #}
    <section class="section">
    {% if header_left|render or header_right|render %}
      
        <header class="content-top content-header--wrapper">
          <div class="content-header--top row">
            {% if header_left %}
              <{{ header_left_wrapper }}{{ header_left_attributes.addClass('content-header--left left col') }}>
                {{- header_left -}}
              </{{ header_left_wrapper }}>
            {% endif %}
            {% if header_right %}
              <{{ header_right_wrapper }}{{ header_right_attributes.addClass('content-header--right right col text-right') }}>
                {{- header_right -}}
              </{{ header_right_wrapper }}>
            {% endif %}
          </div>
          {% if header %}
            <div class="content-header--bottom">
              <{{ header_wrapper }}{{ header_attributes.addClass('content-header w-100 pt-2') }}>
                {{ header}}
              </{{ header_wrapper }}>
            </div>
          {% endif %}
        </header> 
      
    {% endif %}
    <div class="full_judgment content drop-shadow top-right-radius white">
      <div class="beige top-right-radius">
        <h3>Full Judgment</h3>
      </div>
      <div class="white thin-border">
        {{ right }}
      </div>
    </div>
  </section>
  </{{ right_wrapper }}>

  {# Footer #}
  <{{ footer_wrapper }}{{ footer_attributes.addClass('content-footer') }}>
    {{ footer }}
  </{{ footer_wrapper }}>

</{{ outer_wrapper }}>

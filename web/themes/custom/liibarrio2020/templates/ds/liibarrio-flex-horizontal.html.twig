<{{ outer_wrapper }}{{ attributes }}>
  <{{ content_wrapper }}{{ content_attributes.addClass('d-flex flex-row justify-content-between align-items-center') }}>
    {{ content }}
  </{{ content_wrapper }}>
</{{ outer_wrapper }}>